# Real production Turnstile sitekey (public value, safe to commit — see
# .env for why this isn't a secret). Used by `npm run build` /
# `predeploy`; local `npm run dev` uses .env's test sitekey instead.
VITE_TURNSTILE_SITEKEY=0x4AAAAAAD7pmL8jpkIcN2lG
